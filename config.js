const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "password",
        database: "weldorkdb",
    },
    website: {
        hostname: "http://127.0.0.1:3000"
    }
};

module.exports = config;